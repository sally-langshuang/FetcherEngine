package leet

func longestPalindrome(s string) string {
	//lastOccur := make(map[byte]int)
	var max = s[0:1]
	//todo
	//lastOccur[s[0]] = 0
	//head := 1
	//for i, _ := range s {
	//	if firstIdx, ok := lastOccur[s[i]]; ok {
	//		if i - firstIdx + 1 > len(max) {
	//			max = s[firstIdx: i + 1]
	//		}
	//	}
	//	lastOccur[s[i]] = i
	//
	//}
	return max
}